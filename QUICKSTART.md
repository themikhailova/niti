# 🚀 Быстрый старт NITI Social Network

## За 5 минут до запуска!

### Шаг 1: Установите зависимости

```bash
pip install -r requirements.txt
```

### Шаг 2: Инициализируйте проект

```bash
python init_project.py
```

Этот скрипт автоматически:
- ✅ Создаст необходимые директории
- ✅ Сгенерирует безопасный SECRET_KEY
- ✅ Создаст файл .env с настройками
- ✅ Инициализирует базу данных

### Шаг 3: Запустите приложение

```bash
python app.py
```

### Шаг 4: Откройте в браузере

```
http://127.0.0.1:5000
```

## 🎉 Готово!

Теперь вы можете:
1. Зарегистрироваться (username: только латиница, цифры, _)
2. Создавать посты
3. Искать других пользователей
4. Подписываться на них
5. Загружать аватары

---

## 📚 Полная документация

- **README.md** - подробные инструкции по установке и настройке
- **SECURITY.md** - рекомендации по безопасности
- **CHANGELOG.md** - список всех исправлений
- **security_audit.md** - исходный аудит безопасности

---

## ⚠️ Важно!

### Перед production деплоем:

1. Измените SECRET_KEY в .env на случайный:
   ```bash
   python -c "import secrets; print(secrets.token_hex(32))"
   ```

2. Установите FLASK_ENV=production в .env

3. Используйте PostgreSQL вместо SQLite

4. Настройте HTTPS

5. Прочитайте SECURITY.md!

---

## 🐛 Проблемы?

### База данных не создается?
```bash
flask db init
flask db migrate -m "Initial migration"
flask db upgrade
```

### Ошибка "No module named 'flask'"?
```bash
pip install -r requirements.txt
```

### Ошибка с SECRET_KEY?
Убедитесь что файл `.env` создан и содержит `SECRET_KEY=...`

---

## 📁 Структура проекта

```
niti-social-network/
├── app.py              # Главный файл
├── config.py           # Конфигурация
├── models.py           # Модели БД
├── forms.py            # Формы
├── utils.py            # Утилиты
├── requirements.txt    # Зависимости
├── .env               # Настройки (создается скриптом)
├── templates/         # HTML шаблоны
├── static/            # Статика
│   └── uploads/
│       └── avatars/   # Загруженные аватары
└── logs/              # Логи приложения
```

---

## ✨ Основные возможности

### Безопасность
- ✅ CSRF защита на всех формах
- ✅ Rate limiting (защита от брутфорса)
- ✅ Безопасная загрузка файлов
- ✅ Хеширование паролей
- ✅ Session security

### Функционал
- ✅ Регистрация и авторизация
- ✅ Создание и удаление постов
- ✅ Подписки (follow/unfollow)
- ✅ Поиск пользователей
- ✅ Загрузка аватаров
- ✅ Персональная лента
- ✅ Пагинация

### UI/UX
- ✅ Responsive дизайн (Tailwind CSS)
- ✅ Красивые страницы ошибок
- ✅ Flash сообщения
- ✅ Интуитивный интерфейс

---

## 🎯 Следующие шаги

После запуска рекомендую:

1. Создать несколько тестовых аккаунтов
2. Попробовать все функции
3. Посмотреть логи в `logs/social_network.log`
4. Прочитать полную документацию в README.md
5. Изучить SECURITY.md перед деплоем

---

## 💡 Полезные команды

```bash
# Запуск приложения
python app.py

# Создание миграции после изменения моделей
flask db migrate -m "Описание изменений"

# Применение миграций
flask db upgrade

# Откат миграции
flask db downgrade

# Проверка безопасности зависимостей
pip install safety
safety check
```

---

Удачи! 🚀

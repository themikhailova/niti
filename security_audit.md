# Аудит безопасности и архитектуры социальной сети NITI

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ БЕЗОПАСНОСТИ

### 1. **CSRF уязвимость**
- ❌ Flask-WTF установлен, но CSRF защита НЕ включена
- ❌ В `app.py` используется `FlaskForm` и `{{ form.hidden_tag() }}` только в `edit_profile.html`
- ❌ В остальных формах (login, register, post, follow/unfollow) нет CSRF токенов
- **Риск**: Атакующий может выполнять действия от имени пользователя

**Где исправить**:
- login.html, register.html, feed.html - добавить CSRF токены
- Все POST/DELETE роуты должны проверять CSRF

### 2. **Секретный ключ в открытом виде**
```python
app.config['SECRET_KEY'] = 'key'  # ❌ КРИТИЧНО!
```
- **Проблема**: Слабый, статичный ключ в коде
- **Риск**: Взлом сессий, подделка токенов
- **Решение**: Использовать переменные окружения

### 3. **SQL Injection через поиск**
```python
User.query.filter(User.username.ilike(f'{query}%'))
```
- ❌ Хотя SQLAlchemy экранирует, но опасная практика
- ✅ Используйте параметризованные запросы

### 4. **Отсутствие rate limiting**
- ❌ Нет защиты от брутфорса при логине
- ❌ Нет лимитов на создание постов
- ❌ Нет защиты от парсинга через поиск
- **Риск**: DDoS, spam, перебор паролей

### 5. **Небезопасная загрузка файлов**
```python
ALLOWED_EXTENSIONS = {'png', 'jpg', 'jpeg', 'gif'}
```
- ❌ Проверяется только расширение, не MIME-type
- ❌ Нет проверки содержимого файла
- ❌ Можно загрузить `shell.php.jpg`
- **Риск**: RCE через загрузку веб-шелла

### 6. **Path Traversal в аватарах**
```python
filepath = os.path.join(app.config['UPLOAD_FOLDER'], new_filename)
```
- ❌ Хотя используется `secure_filename`, но нет полной валидации
- ❌ Старый аватар удаляется без проверки пути

### 7. **Отсутствие проверки размера изображения**
```python
img.thumbnail((400, 400))
```
- ❌ Можно загрузить огромное изображение (memory bomb)
- ❌ `MAX_CONTENT_LENGTH` = 4MB недостаточно
- **Риск**: DoS через исчерпание памяти

---

## ⚠️ ПРОБЛЕМЫ БЕЗОПАСНОСТИ (средний приоритет)

### 8. **XSS уязвимость в постах**
```html
<p>{{ post.content }}</p>
```
- ⚠️ Jinja2 автоматически экранирует, НО:
- ❌ Нет sanitization HTML/JS
- ❌ Можно вставить длинные строки (DoS интерфейса)

### 9. **Session fixation**
```python
session['user_id'] = user.id
```
- ❌ Не регенерируется session ID после логина
- **Риск**: Перехват сессии

### 10. **Enumeration атаки**
```python
user = User.query.filter_by(username=username).first()
if user and check_password_hash(user.password, password):
    # ...
else:
    flash('Неверное имя или пароль.', 'error')
```
- ⚠️ Timing attack - можно определить существующие username
- **Решение**: Одинаковое время ответа для существующих/несуществующих

### 11. **Отсутствие валидации пользовательского ввода**
- ❌ Нет проверки длины username
- ❌ Нет проверки сложности пароля
- ❌ Нет проверки длины контента поста
- ❌ Можно создать username с пробелами/спецсимволами

### 12. **Информационная утечка в ошибках**
```python
except:
    flash('Имя пользователя занято.', 'error')
```
- ❌ Пустой except - скрывает все ошибки
- ❌ Раскрывает информацию о существующих пользователях

---

## 🔧 АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ

### 13. **Отсутствие миграций БД**
```python
with app.app_context():
    db.create_all()
```
- ❌ `create_all()` не обновляет схему
- ❌ При изменении моделей - потеря данных
- **Решение**: Flask-Migrate (Alembic)

### 14. **N+1 проблема в запросах**
```python
posts = Post.query.filter(...).all()
# В шаблоне: post.user.username - для КАЖДОГО поста отдельный запрос!
```
- ❌ Нет eager loading
- **Решение**: `.options(db.joinedload(Post.user))`

### 15. **Отсутствие индексов**
```python
class User(db.Model):
    username = db.Column(db.String(150), unique=True, nullable=False)
```
- ❌ Нет индекса на username для поиска
- ❌ Нет индекса на timestamp в Post
- **Решение**: `index=True` или создать составные индексы

### 16. **Проблемы с кодировкой**
- ❌ HTML файлы содержат битые символы (кириллица)
- ❌ Нужно указать `<meta charset="UTF-8">` (есть, но не помогло)
- **Проблема**: Файлы сохранены в неправильной кодировке

### 17. **Дублирование кода в шаблонах**
- ❌ Шапка копируется в feed.html, profile.html, search.html
- **Решение**: Base template с блоками

### 18. **Отсутствие логирования**
- ❌ Нет логов для безопасности (попытки входа, изменения профиля)
- ❌ Нет мониторинга ошибок
- **Решение**: Python logging + ротация файлов

### 19. **Неправильная обработка ошибок 404/500**
- ❌ Нет кастомных страниц ошибок
- ❌ В production будут показываться технические детали

### 20. **Проблемы с производительностью поиска**
```python
popular_users = User.query.order_by(User.followers.count().desc()).limit(5).all()
```
- ❌ `count()` на relationship - очень медленно
- **Решение**: Денормализация или отдельная таблица статистики

---

## 🐛 ФУНКЦИОНАЛЬНЫЕ БАГИ

### 21. **Неправильный подсчет followers**
```python
popular_users = User.query.order_by(User.followers.count().desc())
```
- ❌ Это не работает! `.count()` нельзя использовать в `order_by`
- **Решение**: Подзапрос или func.count()

### 22. **Утечка дескрипторов файлов**
```python
img = Image.open(form.avatar.data)
img.save(filepath, 'JPEG', quality=85)
```
- ❌ Не закрывается файл после чтения
- **Решение**: Context manager или явное `.close()`

### 23. **Race condition при удалении аватара**
```python
if os.path.exists(old_path):
    os.remove(old_path)
```
- ❌ Между проверкой и удалением файл может исчезнуть
- **Решение**: try/except

### 24. **Некорректная работа default_avatar**
```python
avatar = db.Column(db.String(255), nullable=True, default='default_avatar.png')
```
- ❌ Файл `default_avatar.png` не создается
- ❌ В `avatar_url()` возвращается SVG placeholder, но в БД другое значение

### 25. **Неправильная проверка подписки на себя**
```python
if user == g.current_user:
    flash('Нельзя подписаться на себя.', 'error')
```
- ⚠️ Работает, но лучше скрыть кнопку в UI

---

## 📊 ПРОБЛЕМЫ UX/UI

### 26. **Нет подтверждения действий**
- ❌ Отписка происходит без подтверждения
- ❌ Нет возможности удалить пост

### 27. **Отсутствие пагинации в профиле**
```python
posts = Post.query.filter_by(user_id=user.id).order_by(Post.timestamp.desc()).all()
```
- ❌ Загружаются ВСЕ посты пользователя
- **Проблема**: У активного пользователя может быть 10000+ постов

### 28. **Нет обработки пустых постов на клиенте**
```html
<textarea ... required></textarea>
```
- ❌ HTML5 валидация легко обойти
- ❌ Серверная проверка есть, но UX страдает

### 29. **Битая кодировка в комментариях**
```python
# если захочешь искать по нескольким полям
```
- ❌ Русские комментарии в коде отображаются некорректно

---

## 🔒 РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

### НЕМЕДЛЕННО (до деплоя):
1. ✅ Добавить CSRF защиту везде
2. ✅ Вынести SECRET_KEY в .env
3. ✅ Добавить валидацию загружаемых файлов (MIME-type)
4. ✅ Добавить rate limiting (Flask-Limiter)
5. ✅ Регенерировать session ID после логина

### ВЫСОКИЙ ПРИОРИТЕТ:
6. ✅ Исправить кодировку файлов
7. ✅ Добавить Flask-Migrate
8. ✅ Исправить N+1 запросы
9. ✅ Добавить валидацию username/password
10. ✅ Добавить индексы в БД

### СРЕДНИЙ ПРИОРИТЕТ:
11. ✅ Создать base template
12. ✅ Добавить логирование
13. ✅ Добавить кастомные страницы ошибок
14. ✅ Исправить подсчет popular_users
15. ✅ Добавить пагинацию в профиле

### НИЗКИЙ ПРИОРИТЕТ:
16. Улучшить UX (подтверждения действий)
17. Оптимизировать поиск (полнотекстовый)
18. Добавить кэширование
19. Покрыть тестами

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ ЗАМЕЧАНИЯ

### Отсутствующая функциональность:
- ❌ Нет восстановления пароля
- ❌ Нет подтверждения email
- ❌ Нет лайков/комментариев (но вы сказали не предлагать новое)
- ❌ Нет удаления постов
- ❌ Нет редактирования постов
- ❌ Нет приватности (все профили публичные)

### Проблемы конфигурации:
```python
app.run(debug=True)  # ❌ НИКОГДА в production!
```

### SQLAlchemy:
```python
SQLALCHEMY_TRACK_MODIFICATIONS = False  # ✅ Правильно
```

---

## 🎯 ИТОГО

**Найдено проблем**: 29
- 🔴 Критических: 7
- ⚠️ Средних: 12
- 🔧 Архитектурных: 10

**Оценка текущего состояния**: 3/10
**Готовность к production**: ❌ НЕ готов

**Время на исправление критических проблем**: ~4-8 часов
**Время на исправление всех проблем**: ~16-24 часа
